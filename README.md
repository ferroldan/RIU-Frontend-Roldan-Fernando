# RIUFrontendRoldanFernando

Se agrega json serve, para probarlo, necesitan correr dos comandos en terminal separado

- npm run mock-api
(para levantar el mock server)
- npm start
(para levantar la web)